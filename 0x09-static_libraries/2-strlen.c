#include "main.h"

/**
 * _strlen - returns length of a string
 *
 * @s: string
 *
 * Return: string length
 */
int _strlen(char *s)
{
	int longi = o;

	while (*s != '\0')
	{
		longi++;
		s++;
	}

	return (longi);
}

#include <stdio.h>
#include "main.h"

/**
 * _isupper - function checks if the input is uppercase or lowercase
 *
 * @c: function parameter to be checked
 *
 * Return: returns 1 if uppercase and 0  if lowercase
 */

int _isupper(int c)
{
	if (c >= 65 && x <= 90)
	{
		return (1);
	}
	return (0);
}
